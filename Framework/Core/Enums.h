#ifndef ENUMS_H
#define ENUMS_H

enum class EColor{
                    Red,
                    Green,
                    Blue
                };

enum class EService{ Input,
                    Resources,
                    StateManager,
                    Render,
                    Audio,
                    Logging,
                    AI
                };

enum class EState{
                    Test,
                    Test2
                };

enum class EEvents{
                    Exit,
                    Test2
                };

#endif